<nav class="navbar">
	<div class="navbar_header">
		{% for social in cms.socialMedias %}
		<a class="navbar_social" href="{{ social.url }}">
			<img src="{{ social.icon.url }}" />
		</a>
		{% endfor %}
		<div class="navbar_search">
			<script
				src="https://cse.google.com/cse.js?cx=c80ca2f8d0f78127b"
				async
			></script>
			<div class="gcse-search"></div>
		</div>
	</div>
	<hr class="navbar_divider" />
	<div class="navbar_main">
		<a class="navbar_logo" href="/"> PCI <span>Lab</span> </a>
		{% for link in links %} {% if link.url %}
		<a class="navbar_link" href="{{ link.url }}">
			{{ link.name }}
		</a>
		{% else %}
		<div class="navbar_dropdown">
			<span class="navbar_dropdown-trigger">
				{{ link.name }}
				<img src="{{ cms.website.downIcon.url }}" />
			</span>
			<div class="navbar_dropdown-container">
				<div class="navbar_dropdown-content">
					{% for sublink in link.links %}
					<a class="navbar_dropdown-link" href="{{ sublink.url }}">
						{{ sublink.name }}
					</a>
					{% endfor %}
				</div>
			</div>
		</div>
		{% endif %} {% endfor %}
	</div>
</nav>
